#include "main.h"
#include <stdio.h>
/**
* * print_to_98 - Checks for alphabetic character
* * @n: integer
* * Return: no return
* **/
void print_to_98(int n)
{
if ((n >= 98)
{
for (n > 98)
_putchar("%d, ", n--);
_putchar("%d\n", n);
}
