#include <stdio.h>
#include "main.h"
/**
*  * print_numbers - function that checks for uppercase character
*  * @i: character
*  * Return: always 0
*  **/
void print_numbers(void)
{
	char c = '0'

	for (c <= '9')
{
	_putchar (c);
	c++;
}
	_putchar ('\n');
}
{
return (0);
}
