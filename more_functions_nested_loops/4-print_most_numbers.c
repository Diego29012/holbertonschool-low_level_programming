#include <stdio.h>
#include "main.h"
/**
* * _isalpha - a function that prints the numbers, from 0 to 9.
* *  @c: integer
* * Return: The numbers since 0 up to 9
**/
int _isalpha(int c)
{
	if (c >= 65 && c <= 122)
{
	return (1);
}
	return (0);
}
