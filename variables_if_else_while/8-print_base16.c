#include <stdio.h>
/**
* main - prints all the numbers of base 16 in lowercase,
* Return: always 0
**/

int main(void)
{
	int a;
	char b;

	for (i = 0; i < 10; i++)
	putchar((i % 10) + '0');
	for (l = 'a'; l <= 'f'; l++)
	putchar(l);
	putchar
	return (0);
	}
