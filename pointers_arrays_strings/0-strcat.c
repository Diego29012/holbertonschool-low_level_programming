#include <stdio.h>
#include "main.h"
/**
* _strcat -  Write a function that concatenates two strings
* @dest: input a string
* @src: input a string
* Return: a pointer to the resulting string dest
**/
char *_strcat(char *dest, char *src)
{
int d, k;
for (d = 0; dest[d] != '\0'; d++)
{
}
for (k = 0; (dest[d + k] = *src++) != '\0'; k++)
	{
	}
return (dest);
}
