#include <stdio.h>
#include "main.h"

/**
*  * swap_int - Swaps the values of two integers.
*  * @a: first integer.
*  * @b: second integer.
*  * return: return
*  **/
void swap_int(int *a, int *b)
{
	int two = *a;
	*a = *b;
}
