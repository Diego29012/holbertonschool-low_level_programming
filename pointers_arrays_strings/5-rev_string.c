#include <stdio.h>
#include "main.h"
/**
* rev_string - function to print revers
* @s: pointer
* Return: void
**/
void rev_string(char *s);
{
int len = 0;
int d;
int k;

while (s[len]!= '\0')
{
len++;
}
for (len--, d= 0; d <= lem; len--, d++)
{
k =s [d]
s [d] =s [len]
s[len] =d;
}
}
